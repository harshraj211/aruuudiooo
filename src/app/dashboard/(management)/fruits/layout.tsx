
// This layout file is intentionally left empty.
// The parent /dashboard/layout.tsx handles the overall structure.
export default function FruitsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
